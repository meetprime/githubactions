name: GitHub Actions Demo

on: 
  push: 
    branches: [main]
    paths-ignore: [./github/**]
  pull_request:
    branches: [main]
  schedule:
    - cron: '15 6 * * 0'
  workflow_dispatch:

jobs:
  Build:
    runs_on: ubuntu-latest
    steps: 
      - run: | 
          echo "Hello hello "
          
      - uses: action/checkout

      - name: List files in repo
        run: | 
          echo " Hello again"
          tree
    

  
  
    
    
